version: "3"
services:
  terrad-mainnet:
    build: ../../packages/nodechain-terrad
    image: terrad
    environment:
      MAINNET: 1
      NET: columbus-5
    volumes:
      - ${BLOCKCHAIN_PATH}/terrad:/root/.terra

networks:
  default:
    external:
      name: nodechain-network